import React from 'react';
import Bio from './Bio';

export default {
  title: 'Components/Post',
  component: Bio
};

const Template = () => (
    <Bio />
);

export const Default = Template.bind({});
